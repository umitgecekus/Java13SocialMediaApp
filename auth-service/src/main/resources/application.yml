server:
  port: 7070

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/java13AuthDB
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
