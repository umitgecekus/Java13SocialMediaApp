server:
  port: 7071

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/java13UserDB
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update